--DELETE FROM STOCK_ITEM_QUANTITY;
--DROP TABLE STOCK_ITEM_QUANTITY;
CREATE TABLE STOCK_ITEM_QUANTITY(
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 100, INCREMENT BY 1) PRIMARY KEY,
	STOCK_ITEM_ID				INTEGER,
	QUANTITY					VARCHAR(100) DEFAULT NULL,
	DESCRIPTION					VARCHAR(100) DEFAULT NULL,
	CREATEDDATE					TIMESTAMP,
	LASTUPDATEDDATE				TIMESTAMP,
	ISENABLE					BOOLEAN DEFAULT TRUE,
	CONSTRAINT	UNIQUE_STOCK_ITEM_QUANTITY		UNIQUE(STOCK_ITEM_ID)
);


--C:\Users\mmishra\Documents\workspace-sts-3.8.4.RELEASE\db\leamonerp.db