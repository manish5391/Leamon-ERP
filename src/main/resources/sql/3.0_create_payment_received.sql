CREATE TABLE PAYMENT_RECEIVED_INFO(
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 100, INCREMENT BY 1) PRIMARY KEY,
	PARTYINFOID						VARCHAR(100) DEFAULT NULL,
	RECEIVEDPAYMENT					VARCHAR(100) DEFAULT NULL,
	RECEIVEDATE						VARCHAR(100) DEFAULT NULL,
	BREMARK							VARCHAR(500) DEFAULT NULL,
	WREMARK							VARCHAR(500) DEFAULT NULL,

	CREATEDDATE						TIMESTAMP,
	LASTUPDATED						TIMESTAMP,
	ISENABLE						BOOLEAN DEFAULT TRUE
);